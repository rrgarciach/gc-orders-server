'use strict';

module.exports = {
    SECRETS: {
        SESSION: 'orders-secret'
    },

    // your local MySQL credentials
    SEQUELIZE: {
        MYSQL_USERNAME: 'string',
        MYSQL_PASSWORD: 'string',
        MYSQL_DB: 'string',
        MYSQL_HOST: 'string'
    }

};
